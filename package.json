{
  "name": "movie-list-parser",
  "version": "1.0.0",
  "description": "Silly movie list parser",
  "main": "app.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "build": "babel src -d app --presets es2015",
    "watch": "babel src -d app --presets es2015 -w",
    "start": "babel src -d app --presets es2015 -w & sudo nodemon app/app.js"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-core": "^6.13.2",
    "babel-preset-es2015": "^6.13.2",
    "express": "^4.14.0"
  }
}
